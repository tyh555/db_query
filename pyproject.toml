[project]
name = "sql"
version = "0.1.0"
description = "A Python package for working with SQL databases"
readme = "README.md"
requires-python = ">=3.12.2"
dependencies = [
    "mcp[cli]>=1.3.0",
    "mysqlclient>=2.2.7",
]
